# P1-039 — 当前产品就绪度复核

状态：已完成。日期：2026-09-23。

## 目标

在 P1-035 至 P1-038 完成后重新核对当前产品主线，替换 P1-034 已过时的缺口结论，明确哪些已由实现关闭、哪些仍必须依赖真实外部条件。

## 范围

只读核对当前交付物、签名工具链证据、运行二进制散列、Windows 回归证据、已连接物理存储和干净 Windows 测试面；更新当前测试入口、README 和就绪度矩阵。

## 不做什么

不新增功能，不重复真机/大文件/重启测试，不生成正式私钥，不把同一物理磁盘的其他分区冒充独立备份，不卸载 WinFsp，不发布产物。

## 涉及文件

- P1-030、P1-034 至 P1-038 验收证据与当前交付 manifest。
- `README.md`、`docs/TESTING.md`。
- 本任务、验收记录和机器可读当前矩阵。

## 实现

- 将 P1-034 的旧结论按当前状态重分类：签名构建、身份初始化和身份绑定已经实现并用临时身份验证；未关闭的是正式长期身份本身。
- 当前交付仍为 `local-preview`/`local-test`，没有把工具链通过误写成正式发行 APK已完成。
- 当前连接的 C: 与 D: 都属于同一个 NVMe 物理磁盘；无可移动卷或映射网络卷，因此 D: 不满足独立备份条件。
- 当前主机已安装 WinFsp 和 PhoneBridge，无 Windows Sandbox 可执行文件和 Hyper-V PowerShell；P1-037 只完成验收入口，真实首装证据仍缺失。
- `docs/TESTING.md` 的当前缺口入口从历史 P1-034 更新为本任务，README 同步当前边界。

## 测试

- 当前安装器、APK和 README 三项 SHA-256 均与 manifest 一致；APK v2 签名与 manifest 证书摘要一致。
- 当前 Windows 发布 DLL SHA-256 仍为 `F0FF0BADC9DAB5F18B8E7128CCC972EDEDBBC6E450E204551EB199D533381C09`，与 P1-030 的 280/280 回归基线完全相同。
- P1-036 的临时身份初始化正向验证通过；P1-038 的八项失败关闭和完整临时发行构建通过；正式身份/keystore 当前均不存在。
- 物理磁盘数 1，C:/D: 均为 Disk 0，可移动卷 0、映射网络卷 0。
- 机器可读证据：`.audit/runs/P1-039/current-product-readiness.json`。

## 验收结果

通过当前状态复核。没有发现需要继续编写产品运行功能的已确认缺陷；签名工具链缺口已关闭，但稳定第三方 APK仍需真实长期身份。另一个独立证据缺口是无 WinFsp 的干净 Windows 首装。两者都不能在当前单盘、已安装 WinFsp 的主机上真实完成。
