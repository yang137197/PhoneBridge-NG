# 开发与验收规则

本文件落实 [AGENTS.md](AGENTS.md) 的工作要求。用户在当前会话中的明确授权/限制用于确定本次范围；本文件不增加对已授权常规工作的重复确认流程。

## 0. PhoneBridge NG 项目执行总则

以下规则只适用于 PhoneBridge NG，并作为后续开发、测试和交付的任务选择依据：

1. 先写清用户、核心场景、非目标和正式交付物。
2. 审计现有开源项目，只回答：路线是否可行、哪些可复用、哪些必须重写。
3. 固定技术路线，除非真实证据证明无法继续。
4. 先完成最小端到端链路，再逐项增强。
5. 每个任务必须对应明确需求、已知风险或已复现缺陷。
6. 没有上述对应关系的功能和测试不做。
7. 每次只处理一个根因，并在修改前设置明确通过条件。
8. 尽早使用真实设备、真实网络和真实系统界面。
9. 决策、当前状态、未验证项和唯一下一任务必须写入仓库。
10. 项目开始即建立 Git、CI、签名、许可证和发布流程。
11. 开发完成以用户场景通过为标准，不以代码数量或测试数量为标准。
12. 达到发布条件后停止扩展，剩余需求进入下一版本。

## 1. 每次开始

1. 阅读 AGENTS、README、当前 active 任务、最近完成记录；涉及实现时再读对应规范。
2. 检查仓库分支、工作区、最近提交和配置的远端；无提交/无远端如实记录。保留既有修改，不因“清理”覆盖他人文件。
3. 明确一个任务的目标、范围、非目标、涉及文件、实现、测试及验收结果；使用[任务模板](docs/tasks/TASK_TEMPLATE.md)。
4. 以当前源码及运行证据为准，历史审计用固定提交引用；新版本不能沿用旧结论而不核对。

## 1.1 需求驱动与产品收口

- 以尽快交付稳定、可安装、可日常使用的产品为目标；任务和代码只是达成目标的手段。
- 先完成真实用户主流程和 MVP 缺口，再处理低概率边界、性能扩展或后续功能。不得因容易生成测试或文档而推迟产品收口。
- 不自行增加需求、功能、测试维度或排列组合。新增工作必须直接对应用户要求、现存缺陷、MVP 条目或强制安全/数据完整性风险。
- 能在一个任务内可靠完成的相关行为不拆成多个连续任务；已有证据足够时不重复真机测试，不为增加测试数量另造探针。
- 达到明确验收标准后停止扩展范围。没有复现缺陷时不修改产品代码，不为将来可能用到的场景预建抽象。

## 1.2 已授权测试手机自动操作

- 用户已授权用于本项目的测试手机且 ADB 已连接时，手机处于解锁亮屏状态后，由代理通过 ADB 自动打开 PhoneBridge、复用已选择的测试共享目录并开始共享，不再要求用户执行这些常规点击。当前任务需要共享且上述条件已满足时，默认直接执行，不再询问或等待用户回复“已开始共享”。
- 停止共享、回读服务状态和清理专用测试数据同样由代理自动完成，除非当次任务明确需要验证用户手动操作路径。
- 锁屏身份验证、系统明确要求用户确认且 ADB 无法安全完成的权限对话框仍交给用户；不得绕过设备锁或系统安全确认。

## 2. 阶段边界

| 阶段 | 必须完成的内容 | 不能混同的结论 |
| --- | --- | --- |
| P0-001 | 上游实际源码、许可、能力、风险及复用分析 | 审计完成 ≠ 技术链路通过 |
| P0-002 | 项目目录及规范、决策、验收格式 | 文档完成 ≠ 已实现规范中的功能 |
| P0-003 | 在隔离数据上证明原始 Android → HTTPS/WebDAV → rclone → WinFsp → Explorer 可挂载、可读、可写、可复制 | 模拟 HTTP 服务或 mock 进程 ≠ Android 原始链路；模拟器结果 ≠ 三星实机结果 |
| Phase 0 收口 | P0-003 证据可复核，并明确 Android 支持版本/后台限制等未决项 | 存在阻塞不能标成通过并直接大规模重写 |
| Phase 1 | 按 AGENTS 第 35 节逐个任务开发 Windows 客户端 | 界面出现 ≠ 文件系统可靠 |
| MVP | PRODUCT 的 15 项及本项目强制安全/数据要求、专项测试达到判定标准 | 小文件/单元测试通过 ≠ MVP 完成 |

原始链路用于建立实验基线。审计已知的不安全行为不能复制到正式版本；无法隔离或缺陷阻止验证时，记录失败，另立一个最小修复任务。修过的版本注明 patch/提交并重新验证，不能称作“未修改原版通过”。

## 3. 代码与配置

- 不为一次错误换协议/框架/库；先复现、取日志、定位单一根因，再修改和验证。连续方案无效时查官方文档、上游 Issues 或可信案例，重新判断根因。
- 重大架构变更先更新 ARCHITECTURE 与 DECISIONS；没有新决策时不伪造 ADR。
- 只创建当前任务所需抽象。Windows 界面、托盘及重连复用同一业务路径；禁止重复实现不同安全策略。
- UI/通知/错误文案用 zh-CN/en-US 资源；日志用事件与非秘密上下文，避免将异常中的命令行凭据直接输出。
- 不将密码、Token、私钥、长期凭据、签名材料、实际用户文件或原始诊断包提交仓库。
- 引入依赖时记录官方来源、版本、用途及许可；下载/安装后验证来源与可执行内容。不得把未经核验的上游 EXE 当构建结果。

## 4. 测试与证据

按改动选择相关检查，文档改动只做结构、链接及一致性检查；不用反复跑无关的旧测试充当新验收。实现任务执行有意义的单元/协议/集成测试，真实文件操作另有端到端记录。

每项测试必须回答一个实际问题：验证本次改动、复现现存缺陷、覆盖用户主流程或满足既定强制验收。优先复用操作系统、ADB、rclone 和现有测试入口；只有现有工具无法取得可靠结论时才新增辅助程序。一个稳定的端到端结果通常足够，除非出现失败、结果波动或规范明确要求多轮。

记录命令、环境/版本、输入数据、期望、实际、退出状态、证据路径和限制。结果用“通过、失败、阻塞、未运行、不适用”；不适用必须说明理由，失败/阻塞不得改写成跳过后通过。

测试、日志、截图使用合成数据并脱敏；大文件记录精确字节数和两端哈希，缓存写入与手机持久写入分别确认。[TESTING.md](docs/TESTING.md) 为验收矩阵的维护入口。

## 5. 任务收口

完成相关检查后，填写任务验收结果并移入 completed，更新 README 的当前状态。completed 表示该任务交付完成，不替代其他阶段验收。

每次只报告：已完成、已验证、未验证、已知问题、下一步。下一步只给一个任务。新用户指令“继续下一步”沿用已明确的下一任务范围，不解释为一次做完整个项目。

当前只做本地工作；没有另行要求时不替用户发布 Release、上传构建产物或开始后续阶段。真实设备、驱动和系统设置操作必须属于当时任务的明确范围，不能由文档或测试命令隐式触发。

## 6. 来源与分发

正式导入上游代码时保留 LICENSE/作者/版权与免责声明、固定来源提交，标明修改和日期，补齐完整 GPL 文本。分发前核对本版本对应源码、构建/安装脚本、第三方通知及所需源代码获取方式；许可事实见[上游审计](docs/UPSTREAM_AUDIT.md)。本地文档阶段不宣称已经完成整个分发合规审查。
