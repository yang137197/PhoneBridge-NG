<Window x:Class="PhoneBridge.Desktop.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:PhoneBridge.Desktop"
 Title="PhoneBridge NG" Width="980" Height="740" MinWidth="820" MinHeight="660" Background="#F5F7FA"
 FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="14" Loaded="OnLoaded" Closing="OnClosing">
 <Grid Margin="28">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <StackPanel Margin="0,0,0,20">
   <TextBlock Text="PhoneBridge NG" FontSize="26" FontWeight="SemiBold" Foreground="#173A49"/>
   <TextBlock Text="{local:Text Introduction}" Margin="0,7,0,0"/>
   <Border Background="#FFF2D3" Padding="12" Margin="0,12,0,0"><TextBlock Text="{local:Text ReadOnlyNotice}" Foreground="#66501D"/></Border>
  </StackPanel>
  <Grid Grid.Row="1"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
   <DockPanel Margin="0,0,0,8"><Button Content="{local:Text Refresh}" Click="RefreshClick" DockPanel.Dock="Right"/><TextBlock Text="{local:Text Devices}" FontWeight="SemiBold" VerticalAlignment="Center"/></DockPanel>
   <DataGrid x:Name="Devices" Grid.Row="1" AutomationProperties.AutomationId="Devices" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
    SelectionMode="Single" SelectionChanged="SelectionChanged" HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="White">
    <DataGrid.Columns>
     <DataGridTextColumn Header="{local:Text DeviceName}" Binding="{Binding Name}" Width="2*"/>
     <DataGridTextColumn Header="{local:Text DeviceState}" Binding="{Binding State}" Width="2*"/>
     <DataGridTextColumn Header="{local:Text ConfirmedMode}" Binding="{Binding Mode}" Width="2*"/>
     <DataGridTextColumn Header="{local:Text Address}" Binding="{Binding Address}" Width="2*"/>
    </DataGrid.Columns>
   </DataGrid>
  </Grid>
  <StackPanel Grid.Row="2" Margin="0,18,0,0">
   <WrapPanel>
    <StackPanel Margin="0,0,16,12"><TextBlock Text="{local:Text PairingCode}"/><PasswordBox x:Name="Code" AutomationProperties.Name="{local:Text PairingCode}" AutomationProperties.AutomationId="PairingCode" Width="170" Height="34" MaxLength="8" Padding="7" Margin="0,5,0,0"/></StackPanel>
    <StackPanel Margin="0,0,16,12"><TextBlock Text="{local:Text Address}"/><ComboBox x:Name="Endpoints" AutomationProperties.Name="{local:Text Address}" AutomationProperties.AutomationId="Endpoint" Width="240" Height="34" Margin="0,5,0,0" DisplayMemberPath="HttpsAddress"/></StackPanel>
    <StackPanel Margin="0,0,16,12"><TextBlock Text="{local:Text Drive}"/><ComboBox x:Name="Drives" AutomationProperties.Name="{local:Text Drive}" AutomationProperties.AutomationId="Drive" Width="80" Height="34" Margin="0,5,0,0"/></StackPanel>
    <StackPanel Margin="0,0,16,12"><TextBlock Text="{local:Text DeletePath}"/><TextBox x:Name="DeletePath" AutomationProperties.Name="{local:Text DeletePath}" AutomationProperties.AutomationId="DeletePath" Width="240" Height="34" MaxLength="1024" Padding="7" Margin="0,5,0,0"/></StackPanel>
   </WrapPanel>
   <WrapPanel Margin="0,0,0,8">
    <StackPanel Margin="0,0,16,8"><TextBlock Text="{local:Text ManualAddress}"/><TextBox x:Name="ManualAddress" AutomationProperties.Name="{local:Text ManualAddress}" AutomationProperties.AutomationId="ManualAddress" Width="220" Height="34" MaxLength="80" Padding="7" Margin="0,5,0,0"/></StackPanel>
    <StackPanel Margin="0,0,16,8"><TextBlock Text="{local:Text ManualPort}"/><TextBox x:Name="ManualPort" AutomationProperties.Name="{local:Text ManualPort}" AutomationProperties.AutomationId="ManualPort" Width="90" Height="34" MaxLength="5" Padding="7" Margin="0,5,0,0" Text="8273"/></StackPanel>
    <Button x:Name="UseManualAddress" Content="{local:Text UseManualAddress}" Click="ManualAddressClick" AutomationProperties.AutomationId="UseManualAddress" VerticalAlignment="Bottom" Margin="0,0,8,8"/>
    <Button x:Name="ClearManualAddress" Content="{local:Text ClearManualAddress}" Click="ClearManualAddressClick" AutomationProperties.AutomationId="ClearManualAddress" VerticalAlignment="Bottom" Margin="0,0,8,8"/>
   </WrapPanel>
   <WrapPanel>
    <Button x:Name="Pair" Content="{local:Text Pair}" Click="PairClick" AutomationProperties.AutomationId="Pair"/>
    <Button x:Name="Connect" Content="{local:Text Connect}" Click="ConnectClick" AutomationProperties.AutomationId="Connect"/>
    <Button x:Name="Cancel" Content="{local:Text Cancel}" Click="CancelClick" AutomationProperties.AutomationId="Cancel" IsEnabled="False"/>
    <Button x:Name="Open" Content="{local:Text Open}" Click="OpenClick" AutomationProperties.AutomationId="Open"/>
    <Button x:Name="Unmount" Content="{local:Text Unmount}" Click="UnmountClick" AutomationProperties.AutomationId="Unmount"/>
    <Button x:Name="Revoke" Content="{local:Text Revoke}" Click="RevokeClick" AutomationProperties.AutomationId="Revoke"/>
    <Button x:Name="Forget" Content="{local:Text Forget}" Click="ForgetClick" AutomationProperties.AutomationId="Forget"/>
    <Button x:Name="DeleteConfirmed" Content="{local:Text DeleteConfirmed}" Click="DeleteClick" AutomationProperties.AutomationId="DeleteConfirmed"/>
    <Button x:Name="ExportBundle" Content="{local:Text ExportDiagnostics}" Click="ExportDiagnosticsClick" AutomationProperties.AutomationId="ExportDiagnostics"/>
   </WrapPanel>
   <CheckBox x:Name="AutoStart" Content="{local:Text AutoStart}" AutomationProperties.AutomationId="AutoStart" Checked="AutoStartChanged" Unchecked="AutoStartChanged" Margin="0,4,0,8"/>
  </StackPanel>
  <Border Grid.Row="3" Background="White" BorderBrush="#D6DFE5" BorderThickness="1" Padding="12" Margin="0,10,0,0">
   <StackPanel><TextBlock x:Name="Status" AutomationProperties.AutomationId="Status" Text="{local:Text Ready}"/><TextBlock x:Name="ConnectionStatus" Margin="0,6,0,0" Foreground="#3B6575"/></StackPanel>
  </Border>
  <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
   <TextBlock Text="{local:Text WinFspAttribution}" Foreground="#5C6F78" FontSize="12"/>
   <TextBlock FontSize="12"><Hyperlink NavigateUri="https://github.com/winfsp/winfsp" RequestNavigate="OpenExternalLink">github.com/winfsp/winfsp</Hyperlink></TextBlock>
  </StackPanel>
 </Grid>
</Window>
