# P1-025 MVP 证据映射与常用目录验收

日期：2026-09-22。结果：通过。此任务不降低 MVP 标准，只把范围完全匹配的既有真机证据映射回产品表，并补齐三个常用目录；没有重复大文件、视频、断网或权限模式测试。

## MVP 证据映射

| ID | 结果 | 直接证据 |
| --- | --- | --- |
| MVP-01 | 通过 | P1-001 同 LAN Redmi 3.890 秒发现；本任务当前安装版从启动到 Samsung 挂载 6.352 秒，均低于 30 秒 |
| MVP-02 | 通过 | P1-008 Redmi 与 P1-011 Samsung 的真实一次性配对、设备身份绑定及后续保存记录连接 |
| MVP-03 | 部分通过 | P1-013 已验证自启动命令、隐藏单实例和 Samsung 自动挂载；P1-018 实际 PC 重启后缓存数据恢复，但未组合验证安装版启用自启动后的实际重启和唯一盘符 |
| MVP-04 | 未完成 | 尚无 Android 实际重启、用户解锁并重新共享后的完整恢复证据 |
| MVP-05 | 通过 | 本任务当前安装版逐一打开 DCIM、Download、Pictures，P: 与 Android 独立顶层列表全部对应 |
| MVP-06 | 通过 | P0-010 的 100 MB、P1-016 的 1 GB、P1-017 的 5 GB 均完成双向传输，精确长度和两端独立 SHA-256 一致 |
| MVP-07 | 通过 | P1-009 的 Explorer 小文件及 P1-016/P1-017 的大文件上传均由 Android 独立确认最终落盘和内容一致 |
| MVP-08 | 通过 | P1-022 经 P: 重命名后 Android 端新名称内容保持、旧名称消失 |
| MVP-09 | 通过 | P1-009 Explorer 创建并重命名目录，Android 独立确认目标存在 |
| MVP-10 | 通过 | P1-009 过期确认拒绝且目标保留；新确认后文件与目录删除，Android 独立确认不存在 |
| MVP-11 | 通过 | P1-016 在 1 GB 上传期间关闭 Wi-Fi，Windows 客户端未崩溃，手机最终目标不存在，本地缓存明确为 Dirty |
| MVP-12 | 通过 | P1-010 按同一身份自动恢复唯一 P:/rclone；P1-016 网络恢复后原会话继续并完成脏缓存提交 |
| MVP-13 | 未完成 | P1-010 恢复时手机地址未改变；P1-015 也明确未测试 DHCP 地址变化，不能用普通 Wi-Fi 重连替代 |
| MVP-14 | 未完成 | 尚未实际执行 PC 睡眠/唤醒；要求只核对唤醒恢复和无僵尸资源，不要求睡眠期间传输 |
| MVP-15 | 未完成 | 尚未在 5,000/10,000+ 条目下按预先确定的阈值测量目录可操作性、UI 响应和读取量 |

当前计数为 10 项通过、1 项部分通过、4 项未完成。P1-018 的实际重启只证明缓存数据恢复，不能与 P1-013 的独立启动命令证据拼接后声称 MVP-03 已完整通过。

## Samsung 常用目录实测

设备为 Samsung SM-S9180/API 36，Android `versionName=0.1.0`；Windows 使用 `%LOCALAPPDATA%\Programs\PhoneBridge NG` 的 P1-024 最终安装版。手机与电脑处于同一 LAN，使用已有 `LY` 配对和安全模式。操作均由 ADB 自动完成，不要求用户重复点击。

| 共享目录 | P: 顶层项目 | Android 独立顶层项目 | 名称集合匹配 | 挂载耗时 |
| --- | ---: | ---: | ---: | ---: |
| DCIM | 3 | 3 | 3/3 | 6.352 秒 |
| Pictures | 19 | 19 | 19/19 | 2.017 秒 |
| Download | 5 | 5 | 5/5 | 2.024 秒 |

每次先停止上一目录共享并确认 P: 消失，再选择下一目录。每轮挂载期间均只有一个 `PhoneBridge.Desktop` 和一个 rclone；读取成功且没有修改目录内容。DCIM 的 6.352 秒也为当前安装版 Samsung 的 MVP-01 提供低于 30 秒的发现、验证和挂载上界证据。

## 回归与清理

本任务没有修改产品代码，未重复运行 P1-024 已通过的 Windows 279/279 和 Android 123 个 Gradle task。最终手机选择恢复为 Music，共享状态为停止；回读无 Android 前台共享服务、8273 监听或 PhoneBridge WakeLock。P: 不存在，rclone 和 Windows 客户端进程均为 0。
