# P1-036 Android 长期签名身份初始化工具验收

日期：2026-09-23。结果：通过限定验收。一次性初始化工具已完成并验证；正式长期私钥尚未生成。

## 已确认

- 初始化固定使用 PKCS12、RSA-4096、SHA256withRSA、`phonebridge-release` 别名和 `CN=PhoneBridge NG Android Release,O=PhoneBridge NG` 证书主题。
- 主 keystore 与备份目录必须是项目外绝对路径且互不包含；目标文件已存在时拒绝覆盖。
- 密码仅从当前进程环境读取，至少 16 个非控制字符；两个 PKCS12 密码必须完全一致。输出身份记录不含密码或环境变量名称。
- 主路径在项目内、备份路径在项目内、目录嵌套、弱密码、密码不同、已有目标、成功后重跑七项负向用例均退出 1。
- 一次性正向测试生成了两份相同 keystore；SHA-256、身份 JSON与证书 SHA-256 均一致，证书为 RSA-4096，别名正确。
- 主目录、主 keystore 和主身份记录关闭权限继承，访问规则仅含当前用户与 LocalSystem。
- 随机测试密码未出现在日志或身份记录。临时测试根目录及私钥已经删除；项目内没有长期发行密钥。已有 `.audit/android-user/debug.keystore` 是明确隔离的 `LocalTest` 测试证书。

机器可读证据：`.audit/runs/P1-036/signing-identity-provisioning-verification.json`。

## 未验证与边界

测试使用系统临时目录下的两个独立兄弟目录，只证明路径分离，未证明不同物理介质。正式操作必须由用户指定独立受保护的备份介质。备份介质可能不支持 Windows ACL，因此脚本只强制主目录 ACL，备份 keystore 依靠强密码和介质保护。

正式长期私钥没有生成，正式发行 APK也没有构建或安装。密钥一旦用于安装，丢失后不能用另一密钥原位升级既有 APK。若提交过程中异常，脚本会保留已提交文件供人工核对，不自动删除或重新生成。
