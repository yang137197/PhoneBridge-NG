# P1-029 PC 睡眠/唤醒恢复验收

日期：2026-09-23。结果：通过。设备为 Samsung SM-S9180/API 36，使用 Android `0.1.0` 和 P1-024 Windows 最终安装版；睡眠期间没有文件传输。

## 基线

`powercfg /a` 确认本机支持传统待机 S3，不支持 S0 低电量待机或混合睡眠。ADB 自动开始 Music 共享，Windows 安装版 6.034 秒挂载唯一 P:。睡眠前 P: 顶层为 2 项，规范清单 SHA-256 为 `08B1BDD1639BB98723357062CB97FCA77EDA505DC12DF54458B4AFF24DFAB851`；桌面客户端、rclone 和 P: 均为一个，没有文件传输或待上传。

验收前固定：不要求睡眠期间传输；恢复事件起 90 秒内原挂载应恢复健康或完成安全重挂载；必须实际读取 P:，并保持配对记录、目录清单和进程唯一性。

## S3 与恢复

Windows System 日志的 Kernel-Power 事件记录本机在 `2026-09-23 09:03:39.946 +08:00` 因 Application API 进入睡眠，Power-Troubleshooter 事件记录在 `09:06:18.603 +08:00` 从低功耗状态恢复，唤醒源为电源按钮；实际睡眠 158.656 秒。

睡眠前最后一次健康成功为 `09:03:36.118`。睡眠时发现候选变为 0，但没有 `MountStateChanged/Stopped` 或 `RcloneExited`。唤醒后客户端在 `09:06:17.611` 记录一次健康失败，mDNS 候选在 `09:06:21.944` 恢复，并在 `09:06:23.152` 健康成功。以系统恢复事件为基准，恢复健康耗时 4.550 秒。

原桌面 PID `27136`（启动于前一日 `17:49:22`）和原 rclone PID `20988`（启动于 `17:49:28`）均跨睡眠保留并保持响应。全过程只有一个桌面客户端、一个 rclone 和一个 P:，没有替换或重复挂载。

## 实际读取与清理

唤醒后 P: 顶层仍为 2 项，规范清单散列与基线一致。实际读取 `.thumbnails\\.database_uuid` 得到 36 字节，SHA-256 为 `1193D1082C29D7BC31D949054301393B5420E4473D4DB779DBD44160C95B0359`。两份 Windows `.pairing` 记录与 `.store.lock` 的散列与 P1-026/P1-028 基线一致。

验收后通过已授权 ADB 停止 Android App，Windows 自动卸载 P: 并退出 rclone；随后退出空闲桌面客户端。最终 Android 共享进程和 8273 监听、P:、rclone、桌面客户端均不存在。原始结构化证据保存在被忽略的 `.audit/runs/P1-029/`。

没有修改产品代码，因此没有重复 Windows 279/279 或 Android 123 个 Gradle task。结论只覆盖本机传统 S3、Samsung 当前共享和无文件传输的睡眠/唤醒，不外推 Modern Standby、休眠或传输中睡眠。
