# P0-001 — 上游源码审计

状态：已完成源码审计，等待后续任务；不代表 Phase 0 链路通过。日期：2026-09-19。

## 目标

核对 `ysachin26/PhoneBridge` 实际源码与用户交接说明，生成 `docs/UPSTREAM_AUDIT.md`，完成后停止。

## 范围

许可证、Android 权限与服务、WebDAV、TLS、mDNS、Windows 客户端、rclone/WinFsp、凭据保存、挂载与重连、已有测试、可复用部分与风险；核实当前稳定 .NET LTS。

## 不做什么

不修改上游源码，不构建新 UI，不更换架构或协议，不进入 Phase 0 第二/第三任务或 Phase 1；不安装驱动、不启动手机共享、不挂载真实设备、不运行上游预编译安装器。

## 涉及文件

- `AGENTS.md`：保存用户提供的项目开发交接正文。
- `.gitignore`：排除本地审计克隆及隔离测试环境。
- `docs/UPSTREAM_AUDIT.md`：审计报告。
- `docs/tasks/completed/P0-001-upstream-audit.md`：本任务的完成记录。
- `docs/audit/`：必要的审计复现脚本和验证记录，不属于产品代码。

## 实现

上游克隆放在 `.audit/upstream/`，固定审计提交 `a378fec40561a4d18be2334f4de92ee02a0e7d0c`。以源码调用链、官方文档和隔离测试为依据，区分源码事实、复现结果和未验证推断。

## 测试

上游测试 107 通过、1 个 Non-Windows-only 用例跳过；7 项离线定向复现；21 个 Python 文件语法解析通过；上游已跟踪文件改动为 0。命令、版本、结果和边界见 `docs/audit/VALIDATION.md`。报告引用的源码文件及行号按固定提交校验。

## 验收结果

已完成用户要求的许可证、模块、实现、缺失、风险、复用/重写建议和待实机验证分类，输出 `docs/UPSTREAM_AUDIT.md`。

确认上游许可证声明为 GPL-3.0-or-later，但 LICENSE 非完整正文；当前 .NET 稳定 LTS 为 10。复现了明文凭据、指纹失败放行、挂载就绪判断不足、发现协议降级接受、IP 更新回调缺口、并发重复进程和旧挂载死锁。Android 上传/删除/路径风险仅有源码证据，未作真实设备破坏性复现。

没有修改上游源码、架构或产品功能，没有运行安装器、发布、连接手机或开始 Phase 1。未产生新的正式架构决定，故本轮不提前创建第二任务的 DECISIONS/ARCHITECTURE 等文件。

唯一下一任务：Phase 0 第二任务——建立完整项目结构和开发文档，纳入本审计的风险及第三任务隔离数据验收方案。本轮到此停止。
